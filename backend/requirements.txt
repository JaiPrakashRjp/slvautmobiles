# ── Database / migrations ────────────────────────────────────────────────
SQLAlchemy>=2.0,<3.0
alembic>=1.13
psycopg2-binary>=2.9        # PostgreSQL driver
python-dotenv>=1.0          # load .env

# ── API (vehicles backend) ───────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9     # file uploads (documents)
httpx>=0.27                 # WhatsApp Cloud API calls (Meta Graph)

# ── Auth ───────────────────────────────────────────────────────────────────
bcrypt>=4.0                  # password hashing for user login
PyJWT>=2.8                   # signed bearer tokens (login → Authorization header)

# ── Push notifications ──────────────────────────────────────────────────────
firebase-admin>=6.5          # FCM push (reaches the app even when it's closed)

# ── Production server (systemd runs gunicorn with uvicorn workers) ─────────
gunicorn>=21.2              # process manager used by slv-api / slv-api-dev .service
